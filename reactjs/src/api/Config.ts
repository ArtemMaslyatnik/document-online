
export const baseURL = 'http://localhost/document-online/frontend/web';

export enum AppStructureConfigElement   {
    CATALOG = "/catalog",
    ENUMERATION = '/enumeration',
    DOCUMENT ='/document',
}
